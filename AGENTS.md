# ACO — Codex instructions

This checkout is the generic ACO library, NOT a client project or private knowledge store. Do not write filled contexts, client work, keys, session history or private provider IDs here. When asked to use ACO for another task, work in that task's own repository/folder.

Read `CHATGPT.md`, the relevant section of `ACO-INDEX.md` and only the relevant `skills/aco-*/SKILL.md` and canonical role methods. Native optional profiles are generated with `aco_` names. Do not assume a role file was spawned as an independent process; use actual delegation tools only when available.

Use the common protocols in `skills/aco-office-concierge/references/protocols/`. Start with `CONTEXT-RETRIEVAL.md` for large/private context, `PLANNING-AND-APPROVAL.md` before consequential execution, and `VERIFICATION.md` before completion claims. Resolve organization/activity/client/brand/project scope and create an independent session. Checkpoint substantial work with exact artifacts/tests/results; do not claim that local files updated Drive. Use connected tools for authorized Drive operations, or leave a pending packet. No secret collection, forced push or production deployment without explicit permission.

For library development, edit canonical Markdown or source scripts, then run:
```
python3 scripts/generate.py
python3 scripts/build_manifest.py
python3 scripts/aco_cli.py validate
python3 -m unittest discover -s tests -v
```
Preserve the supplied proprietary `LICENSE` and `.git/`. Do not silently change licensing. Use the reviewed migration script for old ACO repository content, not wholesale deletion. Never remove unrelated/untracked files or alter provider access controls to make tests pass.

The task owner and actual host approval/sandbox policies outrank ACO role preferences. Report partial failures, untested integrations and conflicts explicitly.

## Short actions and early questions
Read the relevant already-authorized material, ask essential missing facts at the first stage, then execute. Use the `INTERACTION.md` and `COMMUNICATIONS.md` protocols under `skills/aco-office-concierge/references/protocols/`. Routine tasks need no office-by-office narration. Full commissioned deliverables remain complete. Finance, Commercial, People, Delivery, Administration, Production and Publishing offices are indexed in ACO-INDEX.md; engineering remains Product Office. WhatsApp via a user-connected MCP is optional and never presumed connected.


## Shared storage gate and optional tool registry
Before any persistent write, read `skills/aco-office-concierge/references/protocols/COMPACT-MEMORY.md`. It replaces prior per-session/event file instructions. Do not create files/folders/registry entities for proposals, ideas, leads, applications or rejections; retain worthwhile items as rows in the existing scoped ACO.md. Active small clients/activities/brands/projects may remain sections. Real requested deliverables are allowed. Separate documents require actual commitment/existence plus independence, size or confidentiality. Never delete existing records automatically.

Use `skills/aco-office-concierge/references/resources/INDEX.md` only when a tool/reference could improve the task, then read the relevant category entry and linked original ACO method. All resources are optional; listing is not installation, access, verified safety or license clearance. Three screenshot names remain identity-gated. Keep actions short, clarify decisive missing facts early, and never upload private material or install dependencies without applicable approval.

Default CLI memory is compact schema 2. Old layout support requires explicit --legacy-memory; never auto-fallback. A local mirror is not a Drive write. Connected sessions update the same canonical remote document ID with revision checking and read-back, or report pending. Do not use the legacy event bridge for compact Drive memory.

## Consolidated methods and harness rules — 0.7.0
Use the task-specific method links in the selected role/office, not every method at startup. Artist growth starts with work, inquiry and capacity; brand approval is not market validation; publication readiness is not tool authorization. `capability-audit` only finds local command paths and never discovers credentials/accounts. Protected standards and learning resources may be reference-only. Do not create new knowledge files for new methods, ideas, proposals or tool bookmarks.


## Execution boundary — 0.7.0

Resolve real actions through the shared `EXECUTION.md` protocol. A role or optional resource does not prove a tool is connected. External sends, publications, applications, deployments, deletions, purchases, signatures and access changes require the exact available capability plus scoped approval where defined. Preserve actual receipts and reconcile unknown outcomes before retrying.
