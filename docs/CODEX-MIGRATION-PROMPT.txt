Update the intended ACO repository using the supplied ACO v0.7.0 package.

Read these instructions FROM THE NEW PACKAGE, not the old checkout.
Confirm the exact source and target directories, current Git branch and origin remote.
The new source is a complete release; preserve its proprietary LICENSE unchanged.

1. Extract the ZIP outside the target repository. Do not commit the outer ZIP folder as a nested ACO-agents directory.
2. Inspect `git status`, current branch, origin and recent log. Preserve .git, history and all unrelated, untracked or private material. Stop if tracked user changes need a decision.
3. From the NEW source folder, run the safe migration preview:
   python3 scripts/aco_cli.py migrate --target /ABSOLUTE/PATH/TO/EXISTING/ACO-agents
4. Review managed-file changes. Stop on unknown hashes/collisions. Do not use git clean, reset --hard, blanket deletion or force push.
5. Apply only through the included migration tool:
   python3 scripts/aco_cli.py migrate --target /ABSOLUTE/PATH/TO/EXISTING/ACO-agents --apply
6. In the TARGET checkout run:
   python3 scripts/generate.py --check
   python3 scripts/aco_cli.py validate
   python3 scripts/aco_cli.py role-stocktake
   python3 scripts/aco_cli.py route-benchmark --input config/routing-final-holdout.json
   python3 scripts/aco_cli.py execution-benchmark --input config/execution-benchmark.json
   python3 scripts/aco_cli.py eval-score --input release/behavioral-simulation.json
   python3 -m unittest discover -s tests -v
7. Verify version 0.7.0; 363 canonical roles; 16 skills; 61 workflows; 97 optional resources; Compact Memory remains default.
8. Verify the execution layer exists: capabilities.json, permission-policy.json, adapter-contract.json, workflow-states.json, execution-benchmark.json; execution-plan / permission-check / receipt-check / adapter-check / workflow-check / execution-summary commands; EXECUTION.md protocol.
9. Confirm that these local commands are validators/planners only and do not claim a live service was connected or invoked.
10. Review staged diff and secret/private-path checks. Preserve legitimate historical/upstream licensing notices. LICENSE must remain byte-identical to the supplied release.
11. If checks pass, I authorize a commit:
   ACO v0.7.0 — Art & Commerce Office
12. Fetch origin. Bring the validated commit to main only if a safe fast-forward or reviewed merge is possible. Stop on divergence/conflicts; do not overwrite remote work. Push normally to origin/main when safe, never force.
13. Report the backup branch, work branch, tests/gates, commit SHA, final git status and actual push result.

Do NOT modify Drive/private knowledge, install optional third-party software, connect accounts, send messages, publish content, submit applications, make payments, sign documents, delete remote data, run paid generation or create schedules as part of this repository update.

A GitHub push does not update locally installed ACO files or reload an existing ChatGPT/Codex session. Local installation is a separate authorized step:
   python3 scripts/aco_cli.py install --apply
Optional native profiles still require an explicit office selection, for example --offices all.
